#include "main.h"
/**
 * print_alphabet_x10 - print out all litters a-z
 * and create a new line 10 times
 * Return: 0
 * Ashraf Atef
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
