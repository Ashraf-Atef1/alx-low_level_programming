#include "main.h"
/**
 * _abs - return absolute number of the input
 * @n: the number
 * Return: the absolute number of the input
 * Ashraf Atef
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
