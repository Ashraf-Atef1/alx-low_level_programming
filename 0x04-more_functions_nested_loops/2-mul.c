#include "main.h"

/**
 * mul - multiply and return the multi of 2 digits
 * @a: first digit
 * @b: the second digit
 * Return: return the multi of 2 digits
 * Ashraf Atef
 */
int mul(int a, int b)
{
	return (a * b);
}
