#include "main.h"

/**
 * _isupper - return 1 if c is a number and 0 if not
 * @c: the letter to check
 * Return: return 1 if a number and 0 if not
 * Ashraf Atef
 */
int _isdigit(int c)
{
    if (c >= '0' && c <= '9')
    {
        return (1);
    }
    else
    {
        return (0);
    }
}
